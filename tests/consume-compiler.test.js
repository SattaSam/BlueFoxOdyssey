const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');const path=require('node:path');const root=path.resolve(__dirname,'..');
const context={console:{info(){},warn(){},error(){}},setTimeout,clearTimeout,setInterval,clearInterval,performance,Date,Math,localStorage:{getItem(){return null},setItem(){},removeItem(){}},addEventListener(){},removeEventListener(){},dispatchEvent(){}};context.window=context;vm.createContext(context);for(const rel of ['data/bible-patterns.js','data/bible-catalog.js'])vm.runInContext(fs.readFileSync(path.join(root,rel),'utf8'),context,{filename:rel});context.BlueFox3D.Missions={normalizeActionType:v=>v};vm.runInContext(fs.readFileSync(path.join(root,'engine/bible-runtime-v0-1-unified.js'),'utf8'),context);const BF=context.BlueFox3D,byId=id=>BF.BibleCatalog.find(m=>m.id===id);
for(const id of ['SURPLUS-01','SURPLUS-02-MINERAL','SURPLUS-02-CRYSTAL','SURPLUS-03','MAT-01','TECH-LAB-01'])test(`${id} compile dans le runtime canonique`,()=>{const m=byId(id),c=BF.bibleRuntime.compileMission(m);assert.ok(c);assert.equal(c.root.children.length,2);assert.equal(c.root.children[0].type,'research');assert.equal(c.root.children[1].type,'research');if(id.startsWith('SURPLUS-'))assert.equal(m.autoPrimaryEligible,false);});
