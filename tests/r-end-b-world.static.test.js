const fs=require('fs'),path=require('path'),assert=require('assert/strict');const root=process.env.BLUEFOX_ROOT||path.resolve(__dirname,'..');const s=fs.readFileSync(path.join(root,'engine/world-engine.js'),'utf8');
assert(s.includes('beginFinalCapsuleSequence(options = {})'));assert(s.includes('this.currentMap?.crashCapsule'));assert(s.includes('beginCinematicOrbit'));assert(s.includes('this.character.root.position.lerpVectors(state.start, state.inside'));assert(s.includes('BlueFox traverse la coque et ses colliders'));assert(s.includes('bluefox-final-fade'));assert(s.includes('classList.add("covered")'));assert(s.includes('bluefox:final-departure-completed'));assert(s.includes('bluefox-final-new-game'));assert(s.includes('BF.playNewGameIntro?.()'));assert(!s.includes('openCapsuleDoor'));assert(!s.includes('capsuleDoor'));
console.log('PASS END-B world: existing capsule + camera orbit + collider-agnostic entry + black fade, no door mechanism');
