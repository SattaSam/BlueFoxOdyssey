const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('node:fs'); const vm=require('node:vm'); const path=require('node:path'); const root=path.resolve(__dirname,'..');
const src=fs.readFileSync(path.join(root,'engine','bible-runtime-v0-1-unified.js'),'utf8');
test('runtime: count unique + bindActivationMap lie le second événement déclencheur',()=>{assert.match(src,/if \(trigger\.uniqueOnly\)/); assert.match(src,/event\.toMapId/); assert.match(src,/if \(mission\.bindActivationMap === true && event\.mapId\)/); assert.match(src,/`bibleActivation:\$\{mission\.id\}`/); assert.match(src,/const required = Math\.max\(1, Number\(mission\.trigger\?\.count\) \|\| 1\)/); assert.match(src,/if \(count >= required\) candidates\.push\(mission\)/);});
